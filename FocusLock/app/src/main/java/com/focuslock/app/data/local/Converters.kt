package com.focuslock.app.data.local

/**
 * Reserved for future complex types (e.g. if we ever store a List<String>
 * or a custom enum directly). All current entities use only Room's natively
 * supported types (String, Int, Long, Boolean), so no converters are needed
 * yet — this file exists as the designated home for them so future additions
 * (e.g. challenge difficulty settings) have an obvious place to go.
 */
class Converters {
    // Intentionally empty for now.
}